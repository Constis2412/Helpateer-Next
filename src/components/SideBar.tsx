import React from "react";

// Getter und setter der state abrufen
const SideBar = () => {
  return (
    <div>
      {/* OnCLick(() => setNeueState(<KartenMap />)) */}
      <div className="col-span-1 bg-primary p-4 h-full"></div>
    </div>
  );
};

export default SideBar;
